# API Partial Template
This package extends your application by the feature of rendering sub templates only and request them over API calls.

#### Installation
`````bin
$ composer require skyline/api-partial-template
`````
